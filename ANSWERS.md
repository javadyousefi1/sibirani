# پاسخ‌ها

### ۱. ساختار داده برای کلیدواژه‌ها و ترجمه‌هایشان

هر کلیدواژه به این شکل ذخیره می‌شه:

```ts
{ id: string; key: string; translations: Record<string, string> }
```

کل داده یه آرایه از این آبجکت‌هاست که به صورت یه JSON تو localStorage نگه داشته می‌شه.

فیلد `translations` یه آبجکت ساده‌ست که کلیدش کد زبانه (`"en"`, `"fa"`, `"es"`). مهم‌ترین مزیت این طراحی اینه که برای اضافه کردن یه زبان جدید هیچ تغییری تو ساختار لازم نیست — فقط شروع می‌کنی به نوشتن تو یه کلید جدید و بقیه برنامه خودش کنارش می‌آد. کلیدواژه‌های قدیمی اون کلید رو ندارن که خوده UI اینو به عنوان «ترجمه‌ای موجود نیست» نشون می‌ده، بدون هیچ migration یا backfill‌ای.

در مقابل مدل رابطه‌ای نرمال‌شده (یه جدول جدا برای ترجمه‌ها با ستون‌های `keyword_id` و `lang` و `value`)، ضعف این روش اینه که برای گرفتن ترجمه‌های یه زبان خاص باید کل آرایه رو scan کنی. ولی در این مقیاس این هزینه ناچیزه و در عوض export/import و فهمیدن داده خیلی راحت‌تره.

فیلد `id` یه UUID‌ه که موقع ساخت کلیدواژه تولید می‌شه. این id مرجع ثابتیه که drag-and-drop و عملیات context و تست‌ها ازش استفاده می‌کنن — و به `key` که کاربر می‌تونه هر وقت خواست تغییرش بده ربطی نداره.

### ۲. مقیاس‌پذیری برای هزاران کلیدواژه و زبان‌های زیاد

اولین و مهم‌ترین مشکل اینه که localStorage اصلاً برای این مقیاس ساخته نشده. حجم ذخیره‌سازیش محدوده (معمولاً ۵ مگابایت)، هر بار که می‌خوای بخونی یا بنویسی کل JSON رو serialize/deserialize می‌کنه، و عملیاتش sync‌ه یعنی main thread رو بلاک می‌کنه. با چند هزار کلیدواژه و چند زبان، همین یه چیز به تنهایی برنامه رو کند می‌کنه. راه‌حل درست اینه که داده بره روی یه backend با دیتابیس واقعی و برنامه از طریق API باهاش حرف بزنه.

**رندر:** بدون virtualization، mount کردن هزاران ردیف روی DOM هر دو صفحه رو سنگین می‌کنه. با TanStack Virtual یا `react-window` فقط ردیف‌هایی که توی viewport هستن render می‌شن. کنار این، جدول ادمین حتماً باید pagination بشه — نه به خاطر رندر، بلکه چون نمایش هزار ردیف همزمان از نظر UX هم معنی نداره.

**سرچ:** سرچ روی لوکال استیت با داده زیاد جواب نمی‌ده. باید بره روی API و حتماً debounce داشته باشه تا با هر کیستروک یه request نزنه. سرور می‌تونه full-text search یا index داشته باشه که نتیجه رو سریع برگردونه.

**Context و re-render:** الان هر ویرایش ترجمه کل آرایه رو تو state عوض می‌کنه و همه ردیف‌ها re-render می‌کنن. با داده زیاد این سربار سنگین می‌شه. راه‌حل اینه که context رو بشکونی تا هر ردیف فقط به داده خودش گوش بده، و ردیف‌ها رو با `React.memo` بپوشونی. در مقیاس بزرگ‌تر یه state manager مثل Zustand که subscription‌های fine-grained داره خیلی بهتر از Context خالی عمل می‌کنه.

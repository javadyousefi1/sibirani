branch=$(git rev-parse --abbrev-ref HEAD)

if [ "$branch" = "main" ]; then
  echo "🚀 pushing to main — running build check..."
  npm run build || { echo "❌ build failed. push aborted."; exit 1; }
  echo "✅ build passed. pushing..."
fi
